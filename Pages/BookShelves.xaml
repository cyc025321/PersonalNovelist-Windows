<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:PersonalNovelist_Windows.ViewModels"
             xmlns:local="clr-namespace:PersonalNovelist_Windows.Pages"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" d:DataContext="{d:DesignInstance Type=viewmodels:BookShelfViewModel}"
             x:Class="PersonalNovelist_Windows.Pages.BookShelves"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <WrapPanel Name="BookShelvePanel">
                <ContentControl Name="aa" Height="Auto" Margin="20"/>
                <TextBlock Text="Old" Margin="10,10"/>
                <TextBlock Text="ListBox" Margin="10,10"/>
                <TextBlock Text="Full of junk" Margin="10,10"/>
            </WrapPanel>
        </Grid>
        <Grid Grid.Row="1">
            <Button Height="20" Width="40" Click="Button_Click"/>
        </Grid>
    </Grid>
</UserControl>
